# 국장 저빈도 및 서비스 전수감사 - 2026-08-26

## 결론

- 8월 26일 국장 신규 진입 0건의 직접 원인은 주문 장애가 아니라 전일 손실 두 건과
  당일 첫 손실을 합쳐 발동한 서킷브레이커를 `당일 3연속 손실`로 간주한 세션 차단이었다.
  같은 세션 확정 손실만 세는 수정은 11:18 UTC 배포됐으며 다음 KRX 세션 실장 검증이
  남아 있다.
- 8월 19일과 24일의 0건은 각각 KOSPI -5.803%, -3.124%의 강한 하락장에서
  일반 롱을 막은 의도된 결과다. 빈도를 위해 이 하한을 낮추지 않는다.
- 별도 전수감사에서 RSI 시간순서 오류와 전일 분봉이 당일 VWAP에 섞이는 오류를
  확인했다. 두 오류 모두 정상 진입을 거짓 차단하거나 잘못 허용할 수 있어 수정했다.
- 8월 전체 국장은 121회 진입, 123회 청산, 35승, 비용 후 -213,634원이다.
  신호 개수를 일괄 늘리는 변경은 기대값 근거가 없어 거부하고, 잘못된 차단만 제거한다.
- 인버스는 국내 8건 평균 -0.622%, 해외 5건 평균 -0.926%라 실거래 승격을 계속
  거부한다. 대신 비용 후 이익 고점을 지키는 후행청산을 그림자 정책에만 추가한다.

## 시장 조건별 국장 실적

| 거래일 | KOSPI | 20일 거래량비 | 레짐 | 진입/청산 | 승 | 순손익 |
|---|---:|---:|---|---:|---:|---:|
| 08-18 | -1.549% | 1.145 | strong_down/normal/normal | 7/7 | 2 | -23,198원 |
| 08-19 | -5.803% | 0.863 | strong_down/normal/normal | 0/0 | 0 | 0원 |
| 08-20 | +5.894% | 0.870 | strong_up/normal/normal | 12/12 | 6 | +53,718원 |
| 08-21 | +0.881% | 1.177 | up/normal/calm | 5/5 | 2 | +8,688원 |
| 08-24 | -3.124% | 0.771 | strong_down/normal/calm | 0/0 | 0 | 0원 |
| 08-25 | +0.684% | 0.842 | up/normal/normal | 8/2 | 0 | -980원 |
| 08-26 | +0.971% | 0.947 | up/normal/calm | 0/6 | 2 | +5,912원 |

8월 12일 이후 전략별 자본가중 순성과는 `VOL` 36건 +0.193%, `VWAP+RSI`
13건 +0.231%, `VWAP+VOL` 10건 +0.114%, `RSI` 2건 -0.304%다. 정상적인
복합·거래량 신호는 보존할 가치가 있지만, 8월 전체 손실과 시장 레짐별 편차 때문에
`volume_spike_ratio`, 포지션 수, 주문 횟수를 일괄 완화할 근거는 없다.

## 확인한 신호 오류

### RSI 시간축 역전

KIS 분봉은 최신 봉부터 내려온다. 기존 계산은 최근 14개 변화로 초기 평균을 만든 뒤
더 오래된 변화들을 Wilder 평활식에 순서대로 넣어 가장 오래된 봉에 가장 큰 가중치를
부여했다. 변화가 전혀 없는 경우도 RSI 100으로 반환했다.

8월 26일 저장된 실제 60개 분봉 재계산 결과:

| 종목 | 기존/저장 RSI | 수정 RSI | 차이 |
|---|---:|---:|---:|
| 102110 | 62.643955 | 39.739369 | -22.904586 |
| 001210 | 50.195876 | 44.848016 | -5.347859 |

수정식은 오래된 봉에서 최신 봉 방향으로 초기 평균과 재귀 평활을 수행한다. 표준 예제
21개 종가의 최종 RSI 62.88071830996241과 일치하며, TA-Lib 0.7.1 결과와 부동소수점
오차 범위에서 동일하다. 횡보 15봉은 TA-Lib처럼 0을 반환한다.

참조:
- <https://ta-lib.org/functions/rsi.html>
- <https://github.com/TA-Lib/ta-lib/blob/main/src/ta_func/ta_RSI.c>

### 전일 봉이 섞인 VWAP

국장 조회는 `include_previous=Y`, 미장 조회는 `include_previous_day=True`로 이동평균과
RSI 워밍업 봉을 확보한다. 기존 VWAP은 이 전체 배열을 사용해 장 초반 당일 가격과
전일 거래를 한 값으로 섞었다. 이제 이동평균, RSI, ATR, 거래량 기준은 전체 이력을
유지하되 VWAP만 가장 최신 현지 거래일로 제한한다.

- 국내: `stck_bsop_date`
- 해외: 미국 거래일 `xymd` 우선, 없을 때 한국 날짜 `kymd`

미장 자정 경계에서 전일 봉의 `kymd`가 당일 미국 거래일과 같아지는 충돌도 회귀
테스트로 고정했다. 고정종목 `auto-run`과 양시장 `liquidity-lab` 모두 같은 수정식을 쓴다.

## 국장 저빈도 판정

- 8월 26일 CB 발동 당시 합산 연속손실은 3이지만 같은 KRX 세션 손실은 1이었다.
  기존 정책은 이를 `post_cb_max_fires_per_session=1`에 소비해 30분 뒤에도 110개가
  넘는 후보 신호를 종가까지 차단했다.
- 앞선 배포의 세션 귀속 수정은 이 발동을 교차 세션으로 분류한다. 30분 보호와 신선한
  KOSPI 회복 조건은 유지하지만 종일 차단에는 쓰지 않는다.
- 운영 DB의 온라인 격리 복제본에서 실제 00:32 UTC 해제 직후를 재생했다. 결과는
  `fire_count=1`, `session_loss_fire_count=0`, `cross_session_fire_count=1`,
  `same_session_loss_streak=1`, `session_stop_exempted=true`, 최종 차단사유 없음이었다.
- 8월 13일과 14일처럼 같은 세션에 실제 3연속 확정 손실이 있으면 종일 차단은 그대로다.
- 다음 KRX 세션에는 `same_session_loss_streak < 3`인 CB 해제 뒤 신규 진입이 가능한지,
  RSI/VWAP 수정으로 신호 분류가 어떻게 달라지는지 평가 109와 새 평가 원장에 연결한다.

## 인버스 그림자 개선

완료된 그림자 거래는 다음과 같다.

- 국내: 8건, 2승, 평균 순성과 -0.622%, 합계 -4.977%p
- 해외: 5건, 0승, 평균 순성과 -0.926%, 합계 -4.630%p

국내 손실 네 건은 청산 전 최대 +0.647%에서 +0.973%까지 상승했고, 해외 SQQQ 한 건은
+1.117%까지 상승한 뒤 -0.808%로 종료됐다. 하락장 방향 탐지뿐 아니라 열린 이익을
보존하는 경로가 부족하다는 증거다.

- 국내 `domestic_momentum_v5`: 비용 후 고점 순성과 +0.50%에서 활성화,
  고점 대비 -0.30% 되밀림에 `inverse_trailing_profit_lock` 그림자 청산
- 해외 `overseas_momentum_v3`: 높은 왕복비용을 반영해 +0.60%에서 활성화,
  고점 대비 -0.30% 되밀림에 그림자 청산
- 손절, 강제손절, +2.5% 익절은 우선순위를 유지한다.
- 발동 기준, 고점 순성과, 고점 낙폭, armed 상태를 거래 컨텍스트와 이벤트에 저장한다.
- 실거래 모드는 계속 `shadow`이며 5청산, 3개 고유 최종세션, 비용 후 양의 기대값 전에는
  승격하지 않는다.

## 서비스 전수감사

### 데이터 및 주문 무결성

- 온라인 스냅샷 `quick_check=ok`, foreign-key 위반 0
- 미종결 체결 0, 음수/과다 수량 0, 체결가 누락 0, 종결상태 미확정 0
- 주문 이벤트 고아 0, 체결된 실행그룹의 `cycle_log` 누락 0
- 8월 1일 이후 최종 KOSPI/Nasdaq 세션 리뷰 누락 0
- 최신 리뷰의 진입 시장세션 컨텍스트 연결률 100%
- 정책평가 원장 overdue 미검토 0, 신규 평가 108~110은 예정일 전 전향검증 중
- 8월 Telegram 실패는 8월 11일 502 한 건뿐이며 재발 없음

### 저장소 동시성

운영 DB는 약 647MB이며 API 로그 986,479행, 사이클 로그 455,273행이다. API 로그는
30일, Telegram 로그는 90일 보존되고 거래·시장·정책 원장은 삭제하지 않는다. 기존
SQLite `DELETE` 저널과 기본 5초 잠금에서 감사 읽기와 쓰기 잠금 충돌이 한 번 재현됐다.

- 저장소 기본을 WAL로 전환
- 모든 저장소 연결의 busy timeout을 30초로 상향
- Python sqlite context 종료 뒤 연결을 명시적으로 close. 기존 운영 PID의 지속 DB FD
  52개와 달리 격리 프로세스 500회 연속 조회 뒤 열린 DB FD 0개 확인
- 온라인 backup API도 같은 대기 정책 사용
- 현재 약 29,458개 free page는 재사용 가능하고 디스크 여유가 142GB라 운영 중
  `VACUUM`은 수행하지 않는다.

### 전략가드 발동 근거 계보

새 자본가중 발동 열의 최초 마이그레이션은 기존 활성 가드에 단순평균을 대체값으로 넣었다.
그 결과 자본가중 손실 때문에 발동한 미장 `VWAP+RSI`가 평균과 자본가중 모두 +0.108%로
표시될 수 있었다. 발동 5분 이내의 `strategy_guard_active` 구조화 이벤트에 정확한 값이
있을 때 이를 우선 복원하고, 이벤트가 없는 구형 가드만 평균 대체값을 유지한다.

- `VWAP+RSI`: 발동 평균 +0.108%, 자본가중 -0.679%
- `VWAP+VOL`: 발동 평균 -0.077%, 자본가중 -0.131%

현재 부호와 차단 근거가 다시 일치하며 이후 새 발동은 원래 정밀도의 값을 직접 저장한다.

### 런타임과 의존성

기존 systemd는 Ubuntu `/usr/bin/python3`를 직접 사용했다. 실제 로드 경로에는 알려진
취약점이 보고된 오래된 `requests`, `urllib3`, `h11`, `certifi`, `python-dotenv`가 있었다.
프로젝트를 새로 해석한 의존성에는 취약점이 없었으므로 다음처럼 격리했다.

- `requirements-runtime.lock`으로 런타임 전이 의존성 고정
- `.venv` 전용 Python과 `scripts/bootstrap_runtime_env.sh`
- `pip check` 통과, 전용 환경 `pip-audit` 알려진 취약점 0
- systemd `NoNewPrivileges`, `PrivateTmp`, read-only home/system 및 `data/`, `logs/`,
  `state/` 쓰기 허용 속성을 선언
- API/Telegram DB 로그 저장 실패를 journald ERROR로 남기는 2차 경로 추가
- `KINVEST_LOG_LEVEL=INFO`로 기동, 시장수집, 체결조정 상태를 journald에서도 확인
- 요청 URL에 자격정보가 포함될 수 있는 `httpx`, `httpcore`, `urllib3`는 WARNING 이상으로
  제한해 애플리케이션 INFO 가시성과 전송 계층 비밀정보 보호를 분리. 별도 logging
  필터·formatter도 일반 메시지와 예외 traceback의 Telegram URL·Bearer 값을 치환
- 기존 `keys/data/logs/state`의 group/other 접근 권한을 제거하고 설치 때마다 재검증;
  새 런타임 파일은 `UMask=0077` 적용
- 고정종목 엔진의 일봉·분봉 KIS 갱신 실패도 journald WARNING으로 대체 기록
- 서비스 유닛은 disabled 상태였으나 이번 감사에서 enable해 재부팅 자동기동을 복구

실제 비허용 경로 쓰기 probe에서는 현재 user systemd manager가 mount 기반
`ProtectHome/ProtectSystem/ReadWritePaths`를 강제하지 않았다. bubblewrap도 비특권 user
namespace 제한으로 실행할 수 없어 설치를 되돌렸고, 데몬에 sudo 권한을 주는 우회는
채택하지 않았다. 따라서 이 속성은 방어 의도 설정이지 현재 보안 경계가 아니다.
`/proc`에서 확인한 `NoNewPrivs=1`, 디렉터리 700, 비밀·DB·백업 600이 현재 실효 경계다.

Bandit은 고위험 0건이었다. 동적 SQL 경고는 상수 테이블 목록과 내부 생성 컬럼만 쓰는
경로였고 외부 입력이 식별자로 들어가지 않는다. ShellCheck와 systemd 유닛 검증도 통과했다.
기존 동적 딕셔너리 타입 때문에 전체 mypy 기준선은 410건으로, 이번 변경의 합격 기준으로
사용하지 않고 치명 Ruff 검사와 런타임 회귀 테스트를 사용했다.

## 검증

- 새 `.venv` 전체 테스트: 866 passed
- 영향 모듈 확대 테스트: 559 passed
- focused RSI/VWAP/가드/SQLite/인버스 테스트: 통과
- `compileall`: 통과
- Ruff `E9,F63,F7,F82`: 통과
- JSON 전체 파싱, ShellCheck, `systemd-analyze --user verify`, `git diff --check`: 통과
- systemd 허용경로 쓰기 smoke: 통과; 비허용경로 거부 probe: 실패(호스트 제약으로 보류)
- `doctor`: vps/paper, dry-run false, paper 주문 제출 활성, 경고 없음

## 배포 후 전향검증

1. 다음 KRX 세션에서 교차일 CB 해제 후 정상 신규 진입이 종일 차단되지 않는지 확인한다.
2. 신규 사이클의 RSI와 당일 VWAP이 수정 계산으로 저장되는지 표본을 남긴다.
3. 8월 26일 미장 정규장에 NPAC/UTZ 정산 주문의 제출 가격, 체결, 취소를 확인한다.
4. 인버스 새 버전은 기존 버전과 분리해 고유 세션, 승률, 비용 후 평균, 이익보호 청산을 평가한다.
5. WAL 전환 뒤 DB lock, API/Telegram 로그 누락, service restart 수를 계속 감시한다.

광범위한 빈도 상향, CB 제거, 인버스 실거래 승격은 위 전향검증 전에는 시행하지 않는다.

## 미장 빈도와 가드 재평가

최근 336시간 확정 청산의 자본가중 순성과는 `VOL+RSI` 11건 -0.484%,
`VWAP+VOL` 28건 -0.131%, `VWAP+RSI` 7건 -0.679%다. 특히 `VWAP+RSI`는
거래별 단순평균이 +0.108%여도 큰 포지션 손실 때문에 총 순손익이 -$129.74다.
현재 네 복합 공식의 활성 가드는 이 손실 비대칭을 반영하고 있으며, 제한적 검증진입만
회복 증거를 수집한다. 거래 횟수를 늘리기 위한 가드 해제는 순기대값과 자본가중 성과가
동시에 양수가 되기 전에는 시행하지 않는다.
